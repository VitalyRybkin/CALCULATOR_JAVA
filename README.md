- Правильно понимаешь и начал пользоваться интерфейсами, в случае с calculateFactory он немного избыточен, но это детали, для тренировки ок
- Когда используется factory в названии, то обычно люди сразу думают про паттерн Factory (https://www.tutorialspoint.com/design_pattern/factory_pattern.htm). Опять же мелочи.

```
задача была поставлена - калькулятор, ООП, SOLID, какой-нибудь pattern,
поэтому я взял factory, делал по видео с youtube. Не похоже на factory?
```

- В Calculations не нужно поле currentComplexNumber, расчет идет только в calculationsRun, локальной переменной complexNumber достаточно
```
не очень разобрался, но попробую
```
- В Calculations.calculationsRun() в первых двух if идет дублирование кода если я правильно вижу, если это так, то такого быть не должно, что-то не так
```
да, исправил
```
- Все поля классов по умолчанию должны быть private или protected, public только константы
```
я вроде их делаю private в основном
```
- Константа logFileType и логика в logResult() немного выглядят противоречиво потому что if (logFileType.equals("csv")) {} никогда не отработает, я понимаю что ты хочешь просто руками менять константу, но обычно мертвый код не пишут, если подразумевается что могут быть разые форматы файла логики, то лучше добавить поле класса и инициализировать его в конструкторе, а сам формат задавать при создании Calculations, тогда в методе logResult() не нужен logFileType
```
в задании такого не было, сделал так, чтобы показать расширяемость программы - 
типа дописал класс и пиши в другой в другой формат файла
```
- Все функции классов должны быть private или protected по-умолчанию, public nолько те, которые вызываются извне
```
сегодня как раз смотрел видео про модификаторы доступа, вроде поправил
```
- В целом хороший щаг вперед, просто тут прога простая и ООП выглядит немного избыточно